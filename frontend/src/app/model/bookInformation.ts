import { BookImageLinks } from './bookImageLinks';

export class BookInformation {
    title: String;
    description: String;
    infoLink: String;
    flagFav: Number;
    imageLinks: BookImageLinks;
}
